{
  "name": "@agent-os-core/browser",
  "version": "0.1.0",
  "description": "Browser automation for agent-os — local Playwright, Browser Use cloud, anti-detect.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "install-browser": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "@agent-os-core/shared": "0.1.4",
    "playwright": "^1.49.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3"
  },
  "peerDependencies": {
    "@agent-os-core/shared": ">=0.1.4"
  }
}
